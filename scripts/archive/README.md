# Archived AWS Scripts

These scripts are retained as migration history only.

- `apprunner/`: retired App Runner deployment, domain, and monitoring helpers.
- `migrations/`: one-off migration and investigation utilities used during the move to ECS.

Production now runs on standard ECS/Fargate behind an ALB and CloudFront. Do not use these scripts for the current production path.
